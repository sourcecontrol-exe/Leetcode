module.exports = {
	sub: (s, k)=>{
		let first = 0, last = s.length;
		let max = Number.MIN_SAFE_INTEGER;
		function helper(){
			while(first<last){
				let s1 = s.splice(first, last)
			}
		}
		helper()
		return max;
	}
}